// configuration for html head
// https://searchenginewatch.com/2016/10/05/complete-guide-to-meta-tags-in-seo/
// https://getbootstrap.com/docs/4.0/getting-started/introduction/

const lang = 'en';
const title = 'blogBust';
const charset = 'utf-8';
const description = null;
const keywords = null;
const author = null;
const viewport = 'width=device-width, initial-scale=1, shrink-to-fit=no';
const robots = null;
const canonical = null;

const extStylesheets = 'assets/global.css';

const HeadConfig = {
  lang,
  title,
  charset,
  description,
  keywords,
  author,
  viewport,
  robots,
  canonical,

  extStylesheets
}

export default HeadConfig;